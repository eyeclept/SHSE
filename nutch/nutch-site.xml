<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>

  <!--
    Generated by SHSE. Per-target TLS overrides are applied here when
    tls_verify: false is set on a crawler target.

    Long-term recommendation: mount your homelab CA cert into the Nutch
    container's JVM trust store (cacerts) rather than disabling verification.
  -->

  <property>
    <name>http.timeout</name>
    <value>10000</value>
  </property>

  <!-- Uncomment to disable TLS hostname verification (dev/bootstrap only) -->
  <!--
  <property>
    <name>http.tls.hostnameVerification</name>
    <value>false</value>
  </property>
  -->

</configuration>
